# Skill Governance

Create a skill only when it reduces repeated error, improves traceability, or encapsulates a real reusable workflow.

Do not create skills for one-off preferences, stale project facts, or broad vague behavior.

Global skills require confirmation.
System skills are read-only.
